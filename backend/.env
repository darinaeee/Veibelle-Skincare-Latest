# URL of your deployed frontend (Vercel)
FRONTEND_URL=https://veibelle-deploy.vercel.app
# Optional: add any secret keys, database URLs, or other env vars if used
# Example:
# DATABASE_URL=postgres://user:password@host:port/dbname